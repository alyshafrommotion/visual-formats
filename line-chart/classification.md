---
name: line-chart-classification
description: How to identify whether an ad should be classified/named as the Line Chart visual format. Use when naming an ad (resolving the Visual Format taxonomy token) or auditing an existing ad's format tag. Distinct from execution-sop.md, which governs how to build one.
---

# Line Chart — Classification Guide

## Definition

A static (usually an image) whose hero is a **plotted line/trend graph**: a curve with data points running across a progression axis (time, cycle, usage, sessions) that visualizes the product's mechanism or the result it delivers. The curve tells the story. Not a self-select lookup table (matching-chart), not a single big proof number (statistic).

## The things that must all be true

1. **A plotted line/curve is the dominant visual.** One trend drawn across a progression axis, not a table, not a bar of stats.
2. **Data points and annotations along the curve carry the message** — labeled values and usually one turning-point callout that reveal the trend or the moment that matters.
3. **It's a progression/trend graph** — change read along a line — not a category-to-product lookup, not branching forks, not one hero number.

## Quick test

Is the thing you remember a line moving across the frame, telling a change-over-time (or over-usage) story? If yes, Line Chart.

## Commonly confused with

- **Statistic:** the hero is one or a few big proof numbers rendered large. Line Chart's hero is the plotted curve and its trajectory; the numbers are points on the line, not the message.
- **Matching-chart:** a self-select category → product lookup table ("which one are you"). No plotted curve.
- **Flowchart:** branching yes/no forks with arrows routing you to an answer. Line Chart has no branches, just one continuous line.
- **Before-and-after:** two discrete states (before → after). Line Chart shows the *continuous progression* between states, plotted.
- **Feature-benefit-pointout:** call-outs on a hero product. Line Chart annotates a graph, not a product.

## What to look for as supporting signals

- An x/y line graph overlaid on a lifestyle or product shot
- Plotted data points with real value labels (temps, weights, scores, dollars, days)
- One turning-point / insight annotation on the curve ("Ovulation confirmed", "day you notice results")
- The graph illustrating *how the product works* (a tracked signal) or *the trajectory of a result* over time

Example in the files folder: Natural Cycles' temperature-curve graph ("Track your fertility in your sleep") laid over a woman checking the app in bed, with plotted overnight temps and an "Ovulation confirmed" callout marking the turning point.

See `execution-sop.md` in this same folder for the full build rules.
